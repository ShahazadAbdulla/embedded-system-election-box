#include <Keypad.h>// Define keypad sizeconst byte ROWS = 4; const byte COLS = 4; // Define keymap (adjust if needed)char keys[ROWS][COLS] = {  {'1', '2', '3', 'A'},  {'4', '5', '6', 'B'},  {'7', '8', '9', 'C'},  {'*', '0', '#', 'D'}};// Connect keypad row and column pinsbyte rowPins[ROWS] = {4, 5, 6, 7}; byte colPins[COLS] = {8, 9, 10, 11}; // Initialize KeypadKeypad keypad = Keypad(makeKeymap(keys), rowPins, colPins, ROWS, COLS);void setup() {  Serial.begin(9600);  Serial.println("Keypad Test Ready! Press any key.");}void loop() {  char key = keypad.getKey();    if (key) { // If a key is pressed    Serial.print("Key Pressed: ");    Serial.println(key);  }}